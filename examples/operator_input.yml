general:
  project_name: "demo_redteam"
  ntp_timezone:
  region: "southeast asia"
  vault_path:
  provisioning_user:
headscale:
  provider:
  vm_username:
  setup:
    dns_provider:
    domain:
    base_domain_local:
    jump_host: true
    exp_time_preauthkeys:
    user_client:
      - "redirector"
      - "c2_srv"
      - "phish_srv"
      - "jumphost"
segment_c2:
  provider:
  dns_provider:
  redir_https:
    count: 1
    domain:
    vm_hostname:
    vm_username:
  redir_dns:
    subdomain:
    vm_hostname:
    vm_username:
  framework:
    vm_hostname:
    vm_username:
    sliver:
      version:
      user_agent:
      uri_path:
    cobaltstrike:
      user_agent:
      archieve_file_path:
      profile:
    havoc:
      profile:
      user_agent:
segment_phish:
  provider:
  domain:
  dns_provider:
  server:
    vm_hostname:
    vm_username:
  redir:
    vm_hostname:
    vm_username:
  evilginx:
    redirect_url:
    path_lures:
    phishlets:
