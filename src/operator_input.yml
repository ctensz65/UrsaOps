general:
  project_name: "demo_idsecconf"
  ntp_timezone: "Asia/Jakarta"
  region: southeast-asia
  vault_path:
headscale:
  provider: azure
  setup:
    dns_provider: cloudflare
    domain: cartensz-explorer.com
    base_domain_local: redteam.local
    exp_time_preauthkeys:
segment_phish:
  provider: aws
  domain: cartensz-explorer.com
  dns_provider: cloudflare
  server:
    instance_name: phish_srv
  redir:
    instance_name: redir_phish
  evilginx:
    redirect_url:
    path_lures:
    phishlets: